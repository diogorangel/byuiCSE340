<div class="container">
  <% if (title) { %>
    <h1><%= title %></h1>
    <% if (message) { %>
      <p><%= message %></p>
    <% } %>
  <% } else { %>
    <p>Sorry, no title was provided.</p>
  <% } %>
</div>